export const selectSinglePost = ({ posts }) => posts.singlePost;

export const selectSinglePostError = ({ posts }) => posts.singlePostError;

export const selectPosts = ({ posts }) => posts.posts;

export const selectPostsTotal = ({ posts }) => posts.postsTotal;

export const selectPostsError = ({ posts }) => posts.postsError;
